# 🥖 دليل تسليم وتطوير مشروع الخبزة الذكي للمبرمج (Khobza Developer & Supabase Guide)

مرحباً بك! هذا الدليل موجه لك ولصديقك المبرمج لربط المشروع بقاعدة بيانات **Supabase** السحابية ومستودع **GitHub** ومتابعة التطوير بكل سهولة واحترافية.

---

## 📌 1. ربط المشروع مع Supabase (خطوة بخطوة - مجاني وسهل جداً)

### الخطوة 1: إنشاء مشروع مجاني على Supabase
1. ادخل إلى الموقع الرسمي: [https://supabase.com](https://supabase.com) وسجّل الدخول مجاناً.
2. اضغط على **"New Project"**.
3. اختر اسم المشروع (مثلاً: `khobza-app`)، وعيّن كلمة سر لقاعدة البيانات، واختر أقرب سيرفر (مثلاً: Frankfurt أو أي سيرفر تختاره).
4. اضغط على **"Create new project"** وانتظر دقيقة حتى يكتمل إنشاء السيرفر.

### الخطوة 2: إنشاء الجداول بضغطة زر واحدة (SQL Editor)
1. من القائمة الجانبية في Supabase، اضغط على **SQL Editor** (أيقونة `>_`).
2. اضغط على **"New Query"**.
3. افتح الملف الموجود في المشروع باسم **`supabase-schema.sql`** وانسخ محتواه بالكامل.
4. الصق المحتوى في مربع الـ SQL في Supabase واضغط على زر **"Run"** الأخضر أسفل اليمين.
5. ستظهر لك عبارة **"Success. No rows returned"**، وهذا يعني أن جميع الجداول والصلاحيات (RLS) تم إنشاؤها بنجاح 100%!

### الخطوة 3: الحصول على مفاتيح الربط (API Keys)
1. من القائمة الجانبية في Supabase، اضغط على **Project Settings** (أيقونة الترس ⚙️) ثم اختر **API**.
2. ستجد أمرين مهمين:
   - **Project URL**: (رابط المشروع، يبدأ بـ `https://xxxx.supabase.co`).
   - **Project API Keys (anon / public)**: (المفتاح العام).

### الخطوة 4: وضع المفاتيح في ملف البيئة (`.env`)
في ملف `.env` داخل المشروع:
```env
VITE_SUPABASE_URL="ضع_رابط_المشروع_هنا"
VITE_SUPABASE_ANON_KEY="ضع_المفتاح_العام_هنا"
```
بمجرد وضع هذين المفتاحين، سيقوم التطبيق تلقائياً وبشكل فوري بمزامنة كافة الحسابات، المندوبين، الطلبات، الاشتراكات، وحظر الأرقام مع سحابة Supabase ومزامنتها لحظياً (Real-Time)!

---

## 🐙 2. رفع المشروع إلى GitHub (خطوات المبرمج)

إذا أردت تحميل المشروع وإرساله لصديقك أو رفعه على GitHub:
1. من قائمة الإعدادات أو تنزيل المشروع، حمّل ملف الـ **ZIP** الخاص بالمشروع.
2. فك الضغط عن المجلد.
3. افتح موجه الأوامر (Terminal / Command Prompt) في مجلد المشروع واكتب:
```bash
git init
git add .
git commit -m "الإصدار v1.0.4.final المستقر لتطبيق الخبزة مع سوبابيز"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPOSITORY.git
git push -u origin main
```
4. الآن أصبح المشروع كاملاً على GitHub لدى صديقك!

---

## 💻 3. تشغيل المشروع محلياً على جهاز المبرمج

1. تثبيت الحزم:
```bash
npm install
```
2. تشغيل السيرفر المحلي في وضع التطوير:
```bash
npm run dev
```
3. بناء النسخة الإنتاجية (Production Build):
```bash
npm run build
npm start
```

---

## 📂 4. خريطة الملفات الرئيسية التي يحتاجها صديقك المبرمج

| المسار | الوظيفة |
| :--- | :--- |
| `supabase-schema.sql` | كود بناء جداول Supabase، الصلاحيات، والفهارس. |
| `src/services/supabase.ts` | عميل اتصال ومزامنة Supabase الكامل في الواجهة الخلفية والأمامية. |
| `src/services/pushService.ts` | محرك إرسال الإشعارات الخارجية عندما يكون التطبيق مغلقاً. |
| `public/sw.js` | الـ Service Worker المسؤول عن تشغيل الإشعارات والعمل في الخلفية ووضع الأوفلاين (PWA). |
| `src/services/storage.ts` | إدارة البيانات، حفظ الطلبات، حماية الحسابات من الحذف، وإعادة الضبط. |
| `src/components/CustomerMainView.tsx` | شاشة العائلة (طلب الخبز، تأكيد وصول المندوب، متابعة الرصيد). |
| `src/components/MandoubView.tsx` | شاشة المندوب (استلام الطلبات، مشاركة الـ GPS، تأكيد التوصيل وبلاغات عدم التسديد). |
| `src/components/AdminView.tsx` | لوحة تحكم المدير (المحاسبة، نقاط الاستعادة، إدارة المندوبين والعوائل، وتعيين المناطق). |

---

## 🔔 5. كيف تعمل الإشعارات الخارجية عند إغلاق التطبيق؟

1. تم تفعيل **Web Push API** و **PWA Service Worker** في ملف `public/sw.js`.
2. عند فتح العائلة أو المندوب للتطبيق، يتم تسجيل اشتراك فريد للهاتف عبر `registerPushSubscription()`.
3. عند قيام المندوب بتوصيل الطلب والضغط على "تم التوصيل"، يتم إرسال إشعار فوري حتى لو كان متصفح العائلة أو التطبيق مغلقاً تماماً، ويصدر الهاتف اهتزازاً ورنيناً مخصصاً لفتح التطبيق وتأكيد الاستلام فوراً!

---

إذا احتاج صديقك لأي استفسار، فإن الكود مكتوب بلغة TypeScript الحديثة ومنظم بالكامل مع تعليقات واضحة وموثقة!
