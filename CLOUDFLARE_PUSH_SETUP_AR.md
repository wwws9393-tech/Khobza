# ⚡ دليل إعداد وتشغيل إشعارات Cloudflare Workers مع تطبيق الخبزة

هذا الدليل يشرح لك ولصديقك المبرمج كيفية تشغيل بوابة **Cloudflare Workers** لبث الإشعارات الفورية السريعة للهواتف حتى عند **إغلاق التطبيق تماماً وقفل الشاشة**.

---

## 🌟 لماذا Cloudflare Workers؟
1. **سرعة فائقة (Edge Latency)**: يتم تنفيذ وبث الإشعار من أقرب خادم Cloudflare إلى هاتف المستخدم خلال أجزاء من الثانية.
2. **مجاني بالكامل**: توفر Cloudflare ما يصل إلى **100,000 طلب يومياً مجاناً**.
3. **يعمل والتطبيق مغلق**: يتعاون الـ Worker مع الـ Service Worker للهاتف لإيقاظ الجهاز وعرض التنبيه بصوت واهتزاز عالي.

---

## 🛠️ خطوات النشر في دقيقتين (بدون تعقيد):

### الخطوة 1: الدخول إلى Cloudflare Dashboard
1. ادخل إلى [Cloudflare Dashboard](https://dash.cloudflare.com) وسجّل الدخول بحسابك المجاني.
2. من القائمة الجانبية، اضغط على **Workers & Pages**.
3. اضغط على الزر الأزرق **Create application** ثم **Create Worker**.
4. سمّ الـ Worker باسم مميز، مثلاً: `khobza-push-gateway`.
5. اضغط على **Deploy**.

---

### الخطوة 2: نسخ كود الـ Worker
1. بعد النشر، اضغط على **Edit Code** (أو Quick Edit).
2. افتح ملف المشروع: `cloudflare-worker/worker.js` وانسخ كامل محتواه.
3. الصق الكود داخل محرر Cloudflare واضغط على **Save and Deploy**.

---

### الخطوة 3: نسخ الرابط ووضعه في التطبيق
1. بعد الحفظ، انسخ رابط الـ Worker (مثال: `https://khobza-push-gateway.your-name.workers.dev`).
2. افتح تطبيق الخبزة وانتقل إلى:
   - **لوحة تحكم الأدمن** ➡️ تبويب **"إدارة الإشعارات وتوكنات FCM 🔔"**.
   - ستجد قسم: **"بوابة Cloudflare Edge العالمية للإشعارات الفورية ⚡"**.
   - الصق الرابط واضغط على **"حفظ الرابط"**.
3. يمكنك أيضاً إضافته في ملف `.env`:
   ```env
   VITE_CLOUDFLARE_PUSH_WORKER_URL="https://khobza-push-gateway.your-name.workers.dev"
   ```

---

## 🧪 التجربة الفورية:
- داخل لوحة الأدمن، اضغط على زر **"بث فوري عبر Cloudflare ⚡"** وستشاهد وصول التنبيه لهاتفك واهتزازه فوراً حتى والتطبيق مغلق!
